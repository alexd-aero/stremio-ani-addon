node_modules/
server.log
*.log

# Per-machine autostart files (generated by setup-autostart.ps1 with absolute
# paths for the folder you cloned into — must NOT be committed).
run-forever.cmd
start-hidden.vbs

# Personal publish helper (reads your local PAT path) — keep it out of the repo.
publish-to-github.sh
